import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    txtG:{
        fontSize: 48,
        alignItems: 'center',
    },

    txtM:{
        fontSize: 20,
        alignItems: 'center',
    },

    txtP:{
        fontSize:10,
        alignItems: 'center'
    }

});